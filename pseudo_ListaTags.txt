


    boolean equals(tag de tipo String)
	aux <- primero
	MIENTRAS <aux <> nulo>
	    SI <aux.dato = tag>
		devuelve verdadero
	    FIN SI
	    aux <- aux.siguiente
	FIN MIENTRAS
	devuelva falso